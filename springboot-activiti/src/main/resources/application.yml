
server:
  port: 8002

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/a_activiti?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

  activiti:
    check-process-definitions: false
    process-definition-location-prefix: classpath:/bpmn/
